import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/(admin)/_pathlessLayout/admin')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/(admin)/_pathlessLayout/admin"!</div>
}
